apiVersion: v1
kind: Service
metadata:
  name: voting-service
  labels:
    name: voting-service
    app: demo-voting-app
spec:
  ports:
  - port: 90
    targetPort: 90
  type: LoadBalancer
  selector:
     name: voting-app-pod
     app: demo-voting-app